# Origin of the API server (see ../src/server.ts). The API must have this
# UI's own origin (http://localhost:5173 by default) in its ALLOWED_ORIGIN
# for cross-origin requests with the session cookie to work.
VITE_API_URL=http://localhost:3000
